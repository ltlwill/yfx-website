# 悦斐舜跨境物流官网 · 国内可访问部署指南

## 结论先说

`https://yuefeishun.609738992.workers.dev` 必须翻墙才能访问，**不是配置错误**，是 Cloudflare 的 `workers.dev` 免费域名在中国大陆被 SNI/域名级阻断所致。

- Cloudflare 在中国大陆的节点是与京东云合作的企业级服务，**必须 ICP 备案**才能使用
- 免费账号一律调度到海外节点，`workers.dev` 又是被阻断最严重的后缀
- 手机流量 / 家用宽带 / 公司网络全部打不开 → 已验证，无法靠"换个网络"解决

**必须换托管。** 下面三个方案，都不用改代码（纯静态文件，到哪都能跑）。

---

## 方案对比

| 方案 | 免备案 | 国内访问 | 月成本 | 难度 | 需要域名 |
|---|---|---|---|---|---|
| ① 腾讯云 COS 中国香港地域静态托管 | ✅ | ⭐⭐⭐⭐ 延迟 40-70ms | **约 2-5 元** | 简单 | 不买也能用（默认域名丑） |
| ② 中国香港轻量云服务器 + Nginx | ✅ | ⭐⭐⭐⭐ 延迟 30-60ms | **24-30 元** | 中等 | 建议买 |
| ③ 备案 + 腾讯云/阿里云国内 COS/CDN | ❌ 需备案 | ⭐⭐⭐⭐⭐ 秒开 | 几元 + 域名 | 简单 | 必须买 |

> 备案与否 **只取决于解析到境内还是境外服务器**，跟域名在哪家买无关。买域名 ≠ 要备案。

---

## 方案 ① 腾讯云 COS 中国香港地域（最便宜，推荐先试）

### 费用估算
- 存储 0.156 元/GB/月 — 整站不到 1MB，忽略不计
- 外网下行流量 0.75 元/GB — 按每月 5000 次访问、每次约 500KB 算 ≈ 2.5GB ≈ **2 元/月**
- 请求费用 0.01 元/万次 — 忽略不计

### 操作步骤
1. 打开 https://console.cloud.tencent.com/cos → **创建存储桶**
   - 名称：例如 `yuefeishun-web-1258xxxxx`
   - **地域：务必选「中国香港」**（选错到广州/上海等大陆地域就必须备案）⭐关键
   - 访问权限：**公有读私有写**
2. 进入桶 → **基础配置 → 静态网站** → 开启
   - 索引文档：`index.html`
   - 错误文档：`index.html`（单页站点，避免 404）
3. **上传文件**：把 `website` 文件夹里的全部内容（保持目录结构）上传
   - 推荐装 COSBrowser 客户端图形化拖拽；或用控制台「上传文件」
   - 上传内容：`index.html`、`robots.txt`、`sitemap.xml`、`assets/` 整个目录
4. 复制**静态网站访问域名**，形如：
   ```
   https://yuefeishun-web-1258xxxxx.cos-website.ap-hongkong.myqcloud.com
   ```
5. **用手机流量测试** —— 这一步就能确认国内是否可以直连

### 绑定自有域名（可选）
- 桶 → **域名与传输管理 → 自定义源站域名** → 添加你的域名
- 到域名解析商加一条 **CNAME** 指向上一步的 COS 默认域名
- 这是直接 CNAME 到香港 COS，**不走 CDN，免备案**
- HTTPS：可在 COS 控制台配置自有证书；若控制台提示需要备案，说明桶地域选成了大陆，重新确认
- 之后把域名发我，我改 `robots.txt` / `sitemap.xml` 里的占位域名

### 优点 / 缺点
- ✅ 极便宜、无需运维、天然扛流量
- ❌ 默认域名难看，不适合印在名片上；自定义域名的 HTTPS 配置稍繁琐

---

## 方案 ② 中国香港轻量云服务器 + Nginx（最可控）

### 费用
- 腾讯云轻量 中国香港 2核2G / 30Mbps ≈ **¥30/月**
- 阿里云轻量 中国香港 2核1G / 30Mbps ≈ **¥24/月**
- 都支持微信/支付宝，购买时需账号实名（**实名 ≠ 备案**）

### 操作步骤
1. 买轻量服务器，地域选「中国香港」，系统选 **Ubuntu 22.04**
2. 装 Nginx：
   ```bash
   sudo apt update && sudo apt install -y nginx
   ```
3. 建站点目录并上传文件：
   ```bash
   sudo mkdir -p /var/www/yfx-website
   # 用 SFTP（FileZilla / WinSCP）把 website 文件夹内容传上去
   ```
4. 写配置 `/etc/nginx/sites-available/yfx-website`：
   ```nginx
   server {
       listen 80;
       server_name 你的域名 www.你的域名;

       root /var/www/yfx-website;
       index index.html;

       location / {
           try_files $uri $uri/ /index.html;
       }

       # 静态资源缓存一年
       location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff2)$ {
           expires 1y;
           add_header Cache-Control "public, immutable";
       }

       gzip on;
       gzip_types text/css application/javascript image/svg+xml application/json;
       gzip_min_length 1024;
   }
   ```
5. 启用并重载：
   ```bash
   sudo ln -s /etc/nginx/sites-available/yfx-website /etc/nginx/sites-enabled/
   sudo nginx -t && sudo systemctl reload nginx
   ```
6. 免费 HTTPS 证书：
   ```bash
   sudo apt install -y certbot python3-certbot-nginx
   sudo certbot --nginx -d 你的域名 -d www.你的域名
   ```
7. 域名解析：在域名商加 **A 记录** 指向服务器公网 IP（主域名 + www 各一条）

### 优点 / 缺点
- ✅ 完全可控、HTTPS 一键签发、以后还能挂别的站点或邮箱服务
- ❌ 需要一点运维；有月租

---

## 方案 ③ 备案（长期最优，其实没想象中麻烦）

如果你最终要走这条路：腾讯云/阿里云**全程免费代办**，10-20 个工作日。

### 材料清单
1. 营业执照（副本扫描件，需年检有效）
2. 法定代表人身份证正反面
3. 网站负责人身份证正反面（可以是法人本人，省一步）
4. 域名证书（注册商后台可下载）
5. 网站负责人**幕布拍照**（现在大部分省份支持 App 人脸核验，无需邮寄幕布）
6. 网站名称 + 网站内容说明（填"企业官网 / 跨境物流货运代理信息展示"，不要出现"论坛/博客/新闻"等字眼）
7. 核验单（云平台自动生成，电子签）

### 注意
- 备案期间网站**不能通过国内节点访问**，可以先按方案 ① 顶着
- 备案主体是企业，需要企业对公/法人验证，走一次长期受益
- 备案完成后可迁到腾讯云 EdgeOne / COS 国内节点 + CDN，秒开

---

## 落地建议（我推荐这么走）

1. **今天**：按方案 ① 建个香港 COS 桶，10 分钟上线，手机流量验证国内能打开
2. **本周**：买一个 `.com` 域名（腾讯云/阿里云首年 ¥35-83，或 Cloudflare Registrar 约 $10），CNAME 到 COS
3. **域名定下来**：告诉我，我改 `robots.txt` 和 `sitemap.xml` 里的占位域名 `www.yfs-logistics.com` 并推到 GitHub
4. **以后**：如果国内访问成为瓶颈，再走备案迁国内节点，代码一行不用改

---

## 附：当前线上地址
- Cloudflare：`https://yuefeishun.609738992.workers.dev`（国内需翻墙，仅作海外访问演示用）
- GitHub 仓库：https://github.com/ltlwill/yfx-website
